== README

This is a very basic program to display a username and email address of a user. At this point, the database record must be entered "manually" using the rails console. See bookmarks_2.0 for the interactive version.
